function lookup_user(usr_id, cur_time) {
    time = (new Date()).getTime()/1000
    _lookup_user({'spent':0, 'reg':time})
}

function get_user_ip() {
    return "just_demo_got_nothing"  // Server version has it, but still demo!
}

function cast_vote(spend_addr, spend_time, from_user) {
    
}
